# Main Character V2 — Imported

Start with [ANIMATION_SPEC.md](./ANIMATION_SPEC.md), then inspect the ten PNG strips at 1x and enlarged nearest-neighbour scale.

The approved set is now imported under `game/assets/runtime/player-v2-*`. The seven checkerboard review strips received a controlled transparent-background production pass; the three strips that already had genuine alpha were copied directly. Runtime metadata is regenerated by `tools/generate-player-v2-metadata.ps1`.

These files remain the immutable review sources. The runtime scene now uses the V2 walk/idle clips for all eight directions and selects the matching eight-pose clip during the existing twelve-tick dodge.
